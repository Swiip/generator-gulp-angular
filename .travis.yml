language: node_js

node_js:
  - '0.10'
  - '0.12'
  - iojs

matrix:
  fast_finish: true
  allow_failures:
    - node_js: iojs

before_install:
  - npm install -g npm@latest
  - npm install -g bower
  - npm install -g gulp
  - npm install -g istanbul

after_success:
  - npm run coveralls
